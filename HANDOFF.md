# PROJECT HANDOFF & STATE

**Last Updated By:** Claude Code
**Current Phase:** The run is a real roguelike (branching map, rule-bending opponents, events, blessings, partners). The theme is chosen: «المعزّب — ليلة الأربعين» (`docs/theme.md`). Next: apply the theme's names in the code, three maps (acts), art.

## 1. What We Just Did

### Baloot play and AI
- **التهريب follows the player's video** (`docs/baloot-guide.md` §4 is the source): a discarded suit is NOT wanted and asks for its brother (ديمن → هاص); two suits of one colour, or the led suit's brother, ask for the other colour; climbing in one suit (7→8→بنت) asks for that suit. Reading: `src/ai/beliefs.ts`; sending/answering: `chooseDiscard` / `chooseLead` in `src/ai/play-ai.ts`. The partner answers your signal with its biggest card once it has no winners of its own; a برقية comes first always.
- **حل الحكم:** the side that didn't buy the hokum never leads trumps, unless it holds 4+ trumps or 3+ sure side winners (`defenderMayLeadTrump`, obeyed by the search AI's hard rules too). +0.3 to +0.5 a hand over the previous AI.
- **السوا is part of the game** (not a joker): whenever you lead. Either way the rest plays itself out; a wrong one hands the whole hand to the other side (full value, doubled if doubled, plus every project and your بلوت: you score zero). The السوا joker just pays a bonus on a right call.
- **اللعب طلوع** (from the player's terms video): when trumps are led in hokum you must follow with a higher trump than the best on the table if you hold one (`legalMoves` in `src/engine/trick.ts`).
- **سوالف الطاولة** (`src/game/chatter.ts`): the computer players react in the words Baloot players use — فن / حكيم when their partner buys, تعيش سنين for a خمسين, بالزنوبة when an Ace is ruffed with a tiny trump, خسرانة, كبوت, الأولى للغشمان, قامت. Your seat never talks for you.
- **السوا is judged by order** (`src/engine/sawa.ts`): right when some order of your cards wins every trick whatever they hold (9 of trumps first to draw the King). The play-out follows that order. A wrong one reads خسرانة on the sheet.
- **الدبل is decided on the first five** (six for the ground card's taker); the last three are dealt after the دبل round. The AI's bars carry a margin for the unseen three (measured so it doubles as often as before).
- **Sun doubling uses the regulation's real 100** (7-2), no longer scaled to the match target.

### The run (src/roguelike/)
- **Branching map** (`mapgen.ts`): 9 rows × 2–3 nodes in 3 lanes, links never cross; row 0 matches, elites and shops from row 3, a shop row before the boss. Targets/gold grow with the row. `pathTo()` finds a route (used by tests). `RunController.getAvailableNodes()`.
- **Opponents** (`opponents.ts`): each is a rule bent against YOU (Balatro boss-blind style), hidden on the map, revealed as you walk in. Every rule's difficulty was measured by simulation (match ≈ −3 a hand, elite ≈ −4.5, boss ≈ −6); rejected rules and why are in the design bible PART 7.
- **العارفين** (match, the doubling opponent the player asked for): before doubling they play the hand out with every card known (`buyerWouldLose` in `src/ai/mcts.ts`) and double exactly the contracts you'd lose — your sun too, at any score. −2.1 a hand; الحكم المقفول stops them completely. Plain "double a lot" was tried many ways and always *helped* the player (a doubled hand is all or nothing and the buyer usually wins) — details in the design bible PART 7.
- **الديوانية** (`events.ts`): six events with a choice each (safe / gamble / price now, pay-off later).
- **شخصيات الخوي** (`partners.ts`): the run opens with picking your partner — الشايب (answers your signals first, never doubles), المتحمس (buys and doubles readily; a hand he buys and makes pays +50%), الجفرة (always dealt an Ace, plays by habit), الغشيم (weak and can't read you, but gold ×1.5 and an extra spoil). Their name replaces «شريكك» at the table. Hooks: `decideBid`/`decideDouble` take an `eager` bar, `PlayContext` has `answerFirst`/`deaf`, `RoundOptions.luckyAces`.
- **بركات الحوت** (`blessings.ts`): at the start of the map, three blessings (one free, two with a price). Permanent for the run, not jokers. The player's taste: no blessing costs a life, no shop discount.
- **Jokers are relics:** no cap (the جيب زيادة upgrade is gone); the table row and shop chips shrink to fit.
- **Item review — the player's verdict:** every joker and item stays. An attempt to cut four was reverted on the player's word, and the reasons are worth keeping: **المخلّي** is a gamble (you risk the trick and get paid for the risk); **الحكم المقفول** and **الوجه البارد** protect you from opponents who double a lot; **زبون مميز** is a good *shop* upgrade (it was only weak as a whale blessing). Don't remove them without asking. المترجم now shows you every player's تهريب instead of making your partner understand you (that's normal play now).

### Theme (docs/theme.md) and art direction (docs/art-direction.md) — chosen by the player
- **«المعزّب — ليلة الأربعين»:** once every forty years hidden ديوانيات open for one night; whoever loses to المعزّب becomes **مرهون** (bound as a host, each with a **شرط** = the opponent rule) until someone beats them. You're looking for your grandfather; everything ends before الفجر.
- **Outside the doors it's night, inside it's a frozen hour** (the player didn't want dark art): the neighbourhood at sunset, Andalusia at midday, the Host's palace at sunrise. Only the map screen is night. The Host has hosted card games since cards came through the Mamluks and Andalusia (naipes ← نائب), so Baloot is just this era's game. Andalusian hostages keep their house keys; three keys (one per map) open the last door.
- Renames (not yet in the code): jokers → **التحف**, gold → **ريال**, lives → **ساعات الليل**, the whale → **الراوي** and his **وصايا**, opponents → **المرهونين**, events → **الطرقات**, shop → **دكّان التحف**.
- **Art:** the player makes the assets with Gemini + Higgsfield from `docs/art-direction.md` (style: fine ink line + layered painterly shading with depth — the player rejected flat colour; palette, per-place lighting, content rules, prompt kit, file specs, the asset list with prompts). Claude reviews uploaded images against its checklist and wires them into the game. The player's reference images (R1–R8) stay with the player.
- The player's lines: a warm legend, not horror; jinn and أم الصبيان are fine; no fortune-telling (العرّاف joker → الدربيل); تحف chosen over غنايم.

### The look (src/scenes/fx.ts) — until the real art lands
- Every scene has a painted dusk backdrop (sky gradient, low sun, zellige lattice, grain), shafts of light and drifting gold dust, and (WebGL only) a camera vignette. The table: a teal zellige top under a lamp, a wooden rim and a sadu band.
- Cards (`CardView`): cream paper face, teal zellige back with the Host's sun, a soft shadow that spreads when the card lifts, and a breathing glow on playable cards (`setPlayable`).
- Motion: cards travel in arcs (`arcTo`) and land askew with a puff; the trick winner flares and motes rise; كبوت gets fireworks and a flash; a won match celebrates.
- Arabic text uses Tajawal (Google Fonts; `main.ts` waits for it up to 2.5 s, then falls back) with a soft dark shadow. Buttons are raised sun-gold tiles.
- All textures are painted into canvases at runtime, with blur done through `shadowBlur` (Safari has no canvas `filter`). When the Higgsfield art is approved, these are the places to swap in images.

### «ليش؟» and the player's notes (the AI-teaching loop)
- The table's «ليش؟ 📝» button (top-left) pauses the game and opens an HTML panel (`src/scenes/notesPanel.ts`): every computer play this hand, newest first, each with why (`src/game/explain.ts`, from the `PlayTrace` that `searchCardTraced` in `src/ai/mcts.ts` records: forced / convention / rules / the search's average margin per candidate / habit / سوا).
- The player writes a note or question about a play; it's saved in localStorage with a full `HandSnapshot` (deal, bids, contract, tricks, every hand, the AI's scores). «انسخ» copies them all as JSON to paste to Claude.
- **When the player pastes notes:** rebuild the moment from the snapshot, decide whether the AI was wrong (check the rules skill and `docs/baloot-guide.md`), fix the rule in `play-ai.ts` / `mcts.ts`, and add the moment as a test so it never comes back. If the AI was right, explain why. Stage 2 (later, if wanted): Claude answering in-game through a small server holding the API key.

### The player's first notes (5), all fixed — `tests/player-notes.test.ts` rebuilds each moment
- A defender led a lone trump Ace into the buyer's hokum → `defenderMayLeadTrump` tightened.
- The partner threw a 10 into the opponents' trick to keep it from being bare → points into their trick weigh 4× in `chooseDiscard`.
- A defender went back into the buyer's opening suit in sun → hard rule.
- The partner discarded from his Ace's suit instead of signalling with the brother suit → hard rule + the signal now comes before feeding a 10 into our own trick.
- A hokum defender didn't cash his side Ace → hard rule.
- The rule AI is +0.7 a hand stronger than before (≈6,900 hands head to head, twice); the search's lead over it is now ≈2.0 a hand (400 hands), and the strength test runs 240 hands.
- Second batch (5 notes, tests 6–10): no underleading your own Ace; no فرنكة in hokum (the Ace plays the first time its suit comes round, even onto the partner's trick); ruff with the تسعة while the ولد is out; no برقية into a partner known void in the suit. The rule AI gained another +0.7 a hand; the search still leads it by ≈1.9.
- The player's corrections to that batch: the under-the-Ace rule became *soft* (a penalty the search can overcome — `SoftRule`), the تسعة ruff waits when someone after may over-ruff (`overRuffRisk`), an Ace may fatten the partner's trick in the last two tricks (تكبير), and playouts break ties on raw points. The rule AI gained another +0.6.
- **The search guesses hidden hands by likelihood, not just possibility** (`likelihood` in `src/ai/mcts.ts`): each guessed deal is weighted by the bidding (a hokum buyer usually has the ولد/تسعة, a sun buyer Aces, a first-round passer rarely held the ground suit's ولد+تسعة) and the signals (no Ace in a suit a discard rejected; an Ace in a suit asked for), and the playouts are averaged by weight. Measured paired over 1,000 hands: +0.14 a hand (± 0.21), ahead in all four blocks — small but steady. To measure a change like this, play each deal twice (with/without) and compare per deal; unpaired runs of 600 are too noisy (±0.4).
- «ليش؟» now shows every trick card by card (tap a computer card for its reason, any card to write about it), every seat's full hand as play began (played cards struck), and a tab for the previous hand once it's over (`getLastHand`, `startHands` in the snapshot).
- الذاكرة now shows four suit tiles between the table and your hand (left in each suit; at level 2 which Ace/10 are still out).

### Where the truth lives
- Theme/story: `docs/theme.md`. Art (style, prompts, asset specs): `docs/art-direction.md`.
- Rules: `.claude/skills/baloot-rules/SKILL.md` (+ `docs/baloot-regulation.md`, `docs/baloot-guide.md`).
- Design: `.claude/skills/bloot-roguelike-design/SKILL.md` — PART 7 covers opponents, map, الديوانية, blessings, سوا, relics.
- Jokers: `docs/jokers.md` is generated — `npx vite-node scripts/gen-jokers-doc.ts`.

## 2. Current Blockers / Open Questions
- **Balance of the uncapped jokers:** with no cap, the build-makers (شيخ القبيلة، المايسترو، الوايلد) scale further. Watch them in playtests.
- **Opponent strength for a human:** the margins were measured with the rule-based AI in the player's seat; a real player may find حرّاس الإكك or المدبّلين harder or easier. Retune from playtests.
- **Hokum doubling** has no score limit (the regulation only limits sun). The player noticed doubles "under 100" — the sun case is fixed; ask if they also want hokum limited. (العارفين doubles your sun at any score on purpose — it's their rule.)
- **Deploys only from `main` or `claude/gamedev-skills-install-9ybem7`** (`.github/workflows/deploy-pages.yml`). Work on another branch shows up on the page only after its PR is merged.

- **Ideas from the player's terms video, waiting on the player:** opponents built on table culture (الجفرة، أهل الرصّة، ياخذون القلم…), and the bidding rules قبلك / ما لك ثالث (a later seat's sun in round 1 can be claimed by an earlier seat who passed, not from your partner; after ولا you can't buy a third time). The terms themselves: `docs/` has no copy — ask the player for the video file if needed.

## 3. Next Steps (Where to pick up)
- **START HERE (cards, Sept 2026):** the player approved the card *direction*: the Art Nouveau K♥ `f469b4c6-dd56-435c-95ee-a68b33af21f5` (teal field, gold arch + stepped corner notch, gold sun disc, roses low on the sides) and the engraved navy back `33af2a02-a25e-4a3b-8074-632a7b68d31a`. **Next action:** one *edit* generation of `f469b4c6…` (it as `image_references`): make the frame's stepped corner notch (top-left and bottom-right) bigger — about a quarter of the card width and a third of its height — drawn *into* the gold frame, with the inner arch redrawn to pass inside it without touching, the notch interior plain teal, the figure and roses clear of it. The code then draws only the rank (Cinzel Bold, gold + ink stroke) and the suit (ink/gold/red layered shape) on that plain teal — see the corner notes below. **Zoom every seam before showing the player** (art-direction §6.4). Then, once approved: the other 11 faces in one batch with the approved card as reference, then port to `CardView.ts`.
- **Higgsfield API checked (Sept 26):** `HF_API_KEY` = `key_id:secret`. The proxy clobbers the `Authorization` header, so send the legacy headers `hf-api-key` / `hf-secret`, and set a `User-Agent` (Python's default gets Cloudflare 1010). Base `https://api.higgsfield.ai`; free cost check: `POST /estimate/<model>` with the same body; uploads: `POST /files/generate-upload-url`. **`nano-banana-pro` (and `/edit`) exist but return `model_disabled` for this account** — the player must enable it (console.higgsfield.ai / support) or top up the MCP plan. Enabled image models + prices per image: Qwen Image 3 Edit 2k 1.2 cr ($0.075, 1–3 refs), Ideogram 4 remix QUALITY 1.6 cr ($0.10, one `image_url`), Recraft 4.1 1k 0.56 cr ($0.035, **text only, no reference**), Recraft 4.1 Pro 2k 3.36 cr ($0.21).
- **Model test on the K♥ notch edit (one image each, ~$0.21), all fail** (`review/kh-model-test.png`): Qwen drew a separate little box in the top-left with a stray diagonal, and left the bottom-right as a small double step; Ideogram changed nothing in the frame; Recraft (from text) is a different hand (bright blue, gold leaf, woodcut) and drew the notches as closed boxes inside the frame. Waiting on the player: enable Nano Banana Pro, or another approach.
- **Qwen try 2, with a shape guide (the player asked, $0.075):** image 1 = `f469b4c6…`, image 2 = a plain diagram of the frame (`scripts/art/notch-guide.py`: teal card, double gold frame with the notch 1/4 card width × 1/3 height from the card edge, inner arch following it). Result `https://d3u0tzju9qaucj.cloudfront.net/bcae3439-0ac1-403d-88c5-41493d701398/c54f8265-d024-4909-98ad-187118f10198.png` (API request `768583b9-0a89-4e92-b420-149ddd90e7c2`, 1248×1872; API outputs are kept ≥7 days only — download it), review sheet `review/kh-qwen-guide.png`. Frame now matches the guide: both steps the right size, outer and inner lines continuous with rounded turns, steps plain teal, same style and king. Issues: the inner arch springs out of the inner frame line at its two ends (it joins the line instead of running separately); at the bottom-right step the robe and a rose run straight into the line (the hand and sword hilt clear it); the king is a little larger and the roses sit in the arch's top corners. Waiting on the player's verdict.
- **Why no Nano Banana on the Higgsfield API:** its catalog ("82 entries listed for sale as of September 22, 2026", 16 image: SOUL, Marketing Studio, Grok, Recraft, Qwen, Ideogram, Z-Image) doesn't sell it; `nano-banana-pro` answers HTTP 503 `model_disabled` (correlation id `4cef08e3620c42d98b2638aefad18a6a`). Marketing Studio's token pricing looks like GPT Image, not Nano Banana. The player rejected the Qwen try-2 result ("سيء") and wants Nano Banana Pro. **Chosen route: Google's Gemini API directly** (`gemini-3-pro-image-preview`, needs billing on the Google project). The player is adding `GEMINI_API_KEY` to the environment. Next session: check the price with Google first, tell the player, then run the K♥ edit with image 1 = `f469b4c6…` (download link in the MCP job) and image 2 = the shape guide (`scripts/art/notch-guide.py`), aspect 2:3, 2K; zoom every seam before showing.
- **OpenRouter route (Sept 26):** the player couldn't set up Google Cloud billing (Saudi Arabia goes through the reseller CNTXT), so images now go through **OpenRouter** (`POST https://openrouter.ai/api/v1/images`, `input_references`, returns b64 + `usage.cost`; script `scripts/art/openrouter-edit.py`, reads `OPENROUTER_API_KEY`). The player pasted the key in chat — it must be rotated and put in the environment instead. **Model test on the K♥ notch edit** (top 4 of the Arena image-edit board, Sept 21 2026, + Nano Banana Pro as baseline; card + shape guide as refs): Nano Banana Pro $0.137 (1696×2528), GPT Image 2.5 Sunburst $0.067 and Flare $0.067 (1024×1536, quality high), MAI-Image 2.6 $0.073 (832×1248; takes one reference only, got the card + words), Grok Imagine 2.0 $0.10 (1664×2496). Total ≈ $0.44. All five draw the step as one continuous double line (a big step up from Qwen). Files: `review/kh-candidates/*.png`, sheet `review/kh-openrouter-test.png`. Claude's pick: **Flare** (cleanest frame, figure whole, roses kept) and **Grok 2.0** (same quality at 2K); Nano Banana Pro cut the robe and sword hand at the bottom-right step and lost the second frame line on the right; MAI is low-res and turned the disc into gold leaf. Common leftover: in most, the inner arch springs from the inner frame line at the top-left (as in the original art). Waiting on the player's pick.
- **Player: "use the best, whatever the price; remove the arch, keep it clean."** Model chosen: **GPT Image 2.5 Flare, quality max** (OpenRouter). Edit of the Flare candidate + a guide without the arch (double stepped frame only), n=2, $0.38 for both (`scripts/art/flare-noarch-edit.py`). Pixel check of the frame lines: version 1 (`review/kh-candidates/flare-max-noarch-alt.png`) has the double line only on the left and bottom (single on top and right) — rejected; **version 2 (`review/kh-candidates/flare-max-noarch.png`, 1024×1536) is double all round** (outer 7 px, gap ~15 px, inner 7 px), both lines continuous around both steps, arch gone, steps plain teal. Small leftover: at the bottom-right step the sleeve band and a leaf come close to the inner line. Sheet: `review/kh-flare-noarch.png`. Waiting on the player's approval; then the other 11 faces with this card as the reference (same model, max).
- **DECK DONE (player approved the K♥ and asked for the rest):** all 12 court faces with GPT Image 2.5 Flare max via OpenRouter, refs = approved K♥ + frameless-arch guide (`scripts/art/gen-court-cards.py`; suits per §8: hearts Andalusia/rose, diamonds Mamluk/gold coin + lotus, clubs Hejaz/olive + jasmine, spades Najd/khanjar + palm fronds; teal field, gold sun disc and the stepped double frame on every card). Checked: both steps and all four corners zoomed on every card, lines continuous. The diamonds, clubs and spades jacks first had the sword sheath touching the bottom-right step line — fixed with one edit each (`scripts/art/fix-jack-swords.py`). Cost: 11 × $0.19 + 3 × $0.19 fixes ≈ $2.67 (OpenRouter total so far ≈ $3.5 of $10). Files: game size `public/assets/cards/{H,D,C,S}-{K,Q,J}.webp` (528×760, cropped from 1024×1536 at y 23..1497 so the frame stays centred), masters `art/cards-master/*.webp` (1024×1536), sheet `review/deck-12.png`. **Next:** draw them in `CardView.ts` — the rank (Cinzel Bold, gold + ink stroke) and the suit shape inside the two stepped teal corners (top-left notch ≈ x 0..~210, y 0..~415 of the 528×760 file; measure the exact line positions from the webp before drawing), mirrored bottom-right; numbers (7–A) stay code-drawn; then a Playwright check at 359×685.
- **Player: why is every card the same colour? Background per suit, and try letting the model draw the K and the suit.** Test on H-K and S-K (edit of the masters, n=2 each, $0.71; `scripts/art/suit-index-test.py`, results `review/suit-test/`, sheet `review/suit-test.png`): ♥ burgundy #5E1F24, ♠ navy #1C2B4A (proposed ♦ keep teal, ♣ deep olive). The model draws a clean gold serif K and the suit (red heart / cream spade, gold outline) in both steps, 180° in the bottom-right, and keeps the double frame intact. S-K version 2 left the bottom spade upright (not rotated) — every generated index must be checked for that. Waiting on the player's pick before regenerating all 12 (≈ $2.3 at n=1). If approved, the code draws no index on court cards; number cards keep the code-drawn index in the same style.
- **Player: suit colour only inside the frame, white outside.** Test ($0.71, `scripts/art/white-margin-test.py`, `review/suit-test/white_*.png`, sheet `review/white-margin-test.png`): colour fills up to the outer gold line (the band between the two lines stays coloured), the margin and both steps are white #FAF7F0; the index is K + suit in the suit colour (crimson for ♥, navy for ♠) with a thin gold outline. All four results correct (bottom-right group rotated 180°, frame continuous). Waiting on the player's go to redo all 12 this way (♦ teal, ♣ deep olive inside; ♦ index crimson, ♣ index dark olive/ink).
- **Player: centre the K and suit in the step, same size top and bottom (the bottom looked smaller).** Measured: the bottom index was 15–19% smaller because the model drew the bottom-right step smaller (≈189×272 vs 237×318 on 1024×1536). Fix: `scripts/art/place_index.py` (+ `measure.py`) takes the model's top-left index, clears both white steps to paper (paper is flat, std ≈1.6), and puts the same index centred in the top-left step and an exact 180° copy centred in the bottom-right one — code touches only plain paper, never art or frame. Demo: `review/suit-test/placed_*.png`, sheet `review/index-placed.png`. For the 12: also ask the model for two equal steps (guide2) and re-roll a card whose steps differ by more than ~5%.
- **Player: centre the index in the step and keep the K inside the frame's line (it poked above the top line).** The step box is now bounded by the frame lines (outer left/top line level → step lines), not the card edge. Found why the corners differ: GPT Image 2.5 Flare always draws the bottom-right step ≈92×200 (on 1024×1536) because the figure's hand/sword is there — it ignored an equal-steps guide, a guide-first order, and a pasted 180° copy (it shrank it back; ≈$1.5 spent on these tries). It does follow the top-left step exactly, so the fix is a **small-steps guide** (`scripts/art/guide_small.py`, steps 95×205 from the outer line, layout = image 1, the card = image 2): result TL 95×205, BR 96×195. `place_index.py` then centres the model-drawn index (cut from a model card's top-left step) in both frame-bounded boxes at one scale, bottom = exact 180° copy. Demo `review/suit-test/symmetric_H-K.png`, view `review/index-symmetric.png`. OpenRouter used $6.64 of $10. Waiting on the player before redoing the 12.
- **Player: the index got too small → make the step bigger.** Guide with steps 150×300 from the outer line (`guide_small.py <field rgb> <out> 150 300`) as image 1 + the card as image 2, and the prompt moves the king's sword hand up to the waist so the bottom-right corner is free: version 1 came out TL 149×300 / BR 150×288 (version 2 missed: BR 119×234 — so expect ~50% hits, measure every card). `place_index2.py` (letter and suit re-stacked with a tight gap, 90% of the frame-bounded step) gives an index of 134×255 px on 1024×1536. Result `review/suit-test/bigstep_H-K.png`, view `review/index-bigstep.png`. OpenRouter ≈ $2.98 left. Waiting on the player's go for the 12.
- **APPROVED CARD DESIGN (player, Sept 26): `review/suit-test/bigstep_H-K.png`** — suit colour only inside the frame (♥ burgundy, ♠ navy; proposed ♦ teal, ♣ deep olive), white paper outside, two equal big steps (≈150×300 from the outer line on 1024×1536), index = model-drawn letter + suit in the suit colour with a thin gold outline, centred in the frame-bounded step, bottom = exact 180° copy (`place_index2.py`). **The other 11 are paused by the player** ("save it, we'll do the rest later"). Recipe per card: `guide_small.py <field rgb> guide.jpg 150 300` → Flare max, image 1 = guide, image 2 = `art/cards-master/<card>.webp`, prompt as in the bigstep test (figure's hand up at the waist, bottom-right corner free) → measure steps, re-roll if they differ by >5% (≈50% hit rate) → index: generate/cut a model-drawn index for that rank+suit (the K♥ one came from `review/suit-test/white_H-K_0.png`) → `place_index2.py`. Budget: ≈$0.19 per try, ≈$2.98 left; the player may need to top up ≈$5.
- **UI buttons (next topic, Sept 26):** examples page `review/ui-buttons.html`, published at https://claude.ai/artifact/Detri4zks1eddUYb7QLNvN. Same language as the approved card: code-drawn stepped frame (steps top-left/bottom-right, step ≈ 0.26×height), thick outer gold line with ink edge + thin inner gold line at an even gap, drop shadow that shrinks on press. Variants: primary burgundy (كمّل، متابعة الرحلة), bid row by meaning (صن sun-gold #E3A33B with a gold disc, حكم navy, أشكل teal, بس/ولا paper), table buttons (سوا olive, تخطّي/ترتيب transparent with gold line), shop (اشترِ with price in ريال, بيع paper, لا خلّه transparent), states normal/pressed/disabled. Labels in Reem Kufi 700 (Google Fonts). The path code (`roundedPath` + `stepPoints` in the page script) ports straight into `makeButton` in `src/scenes/ui.ts` (Phaser Graphics). Waiting on the player's feedback.
- **Illustrated button plates (player: "try images from the API to make them prettier, ornaments and drawings"):** one Flare max sheet, n=2 ($0.36), approved card as style ref: six blank plates (burgundy+roses, sun-gold+sun medallions, navy+palm fronds, teal+lotus, paper+gold vine, olive+olive/jasmine), stepped corners, double gold border, empty centre. Sheets `art/ui-plates-sheet-{1,2}.png`; cut plates (alpha) `review/ui-plates/<set>_<colour>.webp` + `info.json` (slice = ornamented end width in source px, for 3-slice: ends fixed, plain middle stretches; in Phaser use NineSlice with leftWidth/rightWidth = slice). New top section on the artifact page (https://claude.ai/artifact/Detri4zks1eddUYb7QLNvN) shows them with code-written Reem Kufi labels, version toggle 1/2. Note: on short buttons the ends take most of the width, so long labels (اشترِ + price) overlap the ornaments a little. OpenRouter ≈ $2.6 left.
- **Player on buttons:** plate **version 1** is the pick, but only for **menus** (كمّل، ابدأ، متابعة الرحلة، the shop). **Gameplay buttons** (bids, سوا، تخطّي) must be simpler and clearer, and the label on the plates looked "written on a picture". Page v3 (same URL): menu labels set like the card index — gold #F2C96B fill with an ink stroke (`-webkit-text-stroke` + `paint-order: stroke fill`) on dark plates, a deep colour with a gold stroke on light plates, with a font switch (Aref Ruqaa / Amiri / Reem Kufi). Note: `background-clip: text` gold gradients render dark when combined with a CSS filter in Chromium — use a solid fill + text-shadow. Gameplay buttons: solid colour, one 3 px gold border, radius 14, Tajawal 800 32 px, colour by meaning (صن sun-gold + small sun dot, حكم navy, أشكل teal, بس/ولا paper, سوا olive, تخطّي/ترتيب transparent), plus four suit buttons for choosing the hokum suit. Waiting on the player.
- **BUTTONS ARE IN THE GAME (player picked the Ruqaa font):** `makeButton` in `src/scenes/ui.ts` now has two kinds. `kind: "menu"` (default) = the generated plate (version 1) as a Phaser NineSlice built at the plate's own height and scaled, so the ornamented ends keep their shape (`public/assets/ui/plate-<burgundy|sun|navy|teal|paper|olive>.webp`, 128 px tall, slice widths in `PLATES`), label in Aref Ruqaa 700, gold #F2C96B with an ink stroke on dark plates / deep colour with a gold stroke on sun & paper, shrunk to fit the plain middle. `kind: "play"` = plain tile (fill by meaning, one gold line, radius, Tajawal 800); `playToneFor(label)` colours the bid/double choices (صن sun, حكم navy, أشكل teal, بس/ولا paper, دبل/ثري/فور/قهوة burgundy); سوا olive; تخطّي/ترتيب quiet. `disabled` greys a button and drops its input. Every scene's `preload()` calls `preloadUi(this)`. Aref Ruqaa (OFL) is self-hosted in `public/fonts/` with @font-face in `index.html` and awaited in `main.ts`; Tajawal 800 added to the Google Fonts link. Checked in the built game at 359×685 (map node panel, bidding, shop): `review/ingame-buttons.png`. `npm test` 206 passed, `npm run build` ok.
- **Credits:** the Higgsfield MCP balance is 1 credit. The player has ~$10 on the **Higgsfield API** (separate balance) and is adding `HF_API_KEY` / `HF_API_SECRET` to the environment. With those set, call the API directly (platform.higgsfield.ai): first check which models it offers (want Nano Banana Pro) and the price per image, tell the player, and only then generate. Results must still be reviewed and shown the same way.
- **Apply the theme's names** (`docs/theme.md` §3–§8): text-only, no system changes.
- **Higgsfield is connected as an MCP connector** (`mcp__higgsfield__*`, OAuth, ~70 credits on the basic plan at the start). The first reference shots (art-direction §8 step 1), each place with two models, waiting on the player's pick:
  - nano_banana_pro (served as nano_banana_2, 1536×2752, 2 credits): hara `d7908342-e753-47cf-9167-336896092796`, andalus `d6875141-5f66-4d50-b466-491f90d0f409`, qasr `14d34da7-86f1-48c0-8069-9534925d4371`
  - gpt_image_2_5 medium 1k (752×1344, 0.5 credits): hara `4e0245b9-72b7-427e-9b6c-5314a9001356`, andalus `5e27cb37-5ac9-42e3-8af2-897edde84790`, qasr `94c93f78-e483-47fc-ad17-611cbfb63fc0`
  - Result images live on `d8j0ntlcm91z4.cloudfront.net`; a session whose network blocks that host can't download them (show them with `show_generation_by_ids`). Use an environment with full network to fetch, review and wire them in. The approved shots become the style references (`medias` role `image_references`) for every later generation.
  - **Claude's review (§6.4), waiting on the player's word:** nano_banana_pro is the style — it's the only one with the sepia ink line (R4); gpt_image_2_5 gives a lush oil painting with no line at all, too red/orange with little teal, and only 752×1344 (below the 1080×2260 background size). Per place: **andalus (nb) passes** (shading could be a little deeper). **hara (nb) nearly passes** — the lighting is exactly §3, but the distance is Moroccan (mountains, kasbah) instead of Najd. **qasr (nb) fails** — the sun is a flame-rayed emblem, not a smooth burnished-gold disc, and the columns look Egyptian/Greek; gpt's qasr has the right disc and mosaic platform (use it as a composition reference only). Proposed redo: nb qasr (+ optionally nb hara), 2 credits each; balance was 62.5.
  - **Round 2** (nano_banana_pro 4k = 3072×5504, 4 credits each, the player asked for the strongest setting; refs = nb andalus as style + the old shot): **qasr** `4af18b13-ed16-4c7b-9ad3-cae4e6a65cd4` passes (smooth hammered-gold disc, horseshoe arches, mosaic platform, ink line kept); **hara** `e2bbf920-fcee-42b6-9d2b-92e4dddbe979` only half-fixed (Najdi crenellations and vents added, but it copied the old draft, so faint mountains remain). Next hara try: drop the old draft from the references. Balance ≈ 54.5.
  - **Model contest** (hara, same prompt, only nb andalus as style ref, one image each): nano_banana_pro 2k `ef4250b1-e275-43a7-8319-2cf7e378dcb1` (2 cr; the job log says nano_banana_2 but billing says Nano Banana Pro — it's an internal name) **wins**: ink line, §3 sky, Najdi crenellations/vents, flat palm horizon, glimpse of an Andalusian arch through the door; it passes §6.4. gpt_image_2_5 max 2k `c5ac5ce5-a07b-4f4b-8d18-ae6b6a737215` (9 cr) is second: now has linework and the most depth, but heavy red kilims and little teal (breaks 60/30/10) and reads as another hand; maybe for §2.2 story scenes. seedream_v5_pro 2k `6612a343-fc0a-4429-9f9c-cb3ccf0d123d` (2.5 cr) third: good line but greyish, emptier, zellige wall band in a Najdi house. flux_2 max needs the Plus plan (not run). **Proposed reference set:** andalus `d6875141…`, qasr `4af18b13…`, hara `ef4250b1…`; model for everything: nano_banana_pro 2k. Balance 41.
  - **Approved by the player** (recorded in art-direction §6.4). The player flagged AI-looking patterns on rugs/cushions: fixed on hara with an edit pass (hara + a sadu band sheet cut from the player's pattern images, media `9ef9dd72-816f-47e3-907e-4ddf927f2a66`) → `391023fd-948e-42b0-b5c4-5e65e812b625` is now the hara reference: the cushions show real sadu bands, the rest is unchanged; the floor rug under the table is still a little blotchy. The base prompt (§6.1) and negative (§6.2) now ask for simple sadu. Next: cards (§8 step 2, 13 images, ~26 credits) after the player's go. Balance ≈ 39.
  - **Cards generated** (§8 step 2, nano_banana_pro 2k, 2:3 = 1696×2528, crop to 5:7 for `CARD_W/H` 132×190; andalus ref for all, + hara + sadu refs for spades). Frameless on purpose: the code draws one frame and the corner rank/suit so all 12 match. Job ids — hearts K `2341dfd3-7a2f-4d1b-9bcd-c97a2c94958b` Q `7411aa7c-5d91-4237-8d7c-f94036bb1d1c` J `30e2c126-2658-48e7-8526-e91703ba3944`; diamonds K `8614fdd1-02e1-45dc-9ff5-c3951d71084b` Q `5dd03795-9ad9-42e5-a38c-bc03a5175f53` J `f28cfb32-654d-4293-a5f6-b20b4ec1a9eb` (first try was flagged nsfw by Higgsfield and refunded; reworded as a squire in his twenties); clubs K `42edae19-a0fd-4ed0-b2da-1792f92b0224` Q `bdfa7eed-8379-46b5-a7e5-aba2eae132ee` J `f714eec9-6894-4bb2-8066-2548a5dd7ab3`; spades K `9836849d-36a9-4eb8-b0f3-aa7b44881b3a` Q `1f645e85-ab9e-4f4d-b01c-f031466522d2` J `fbbd0317-4a30-4ba4-a790-a6ac32f9d20b`; back `90342243-b0fc-4cf8-8a65-f5fbc0057491`. Review: one hand, courts read by colour at table size, modest; issues: 5 faces have notched/cut corners or a thin inner border (hidden by the code's frame + corner plates), figures are three-quarter length (faces small at 132×190), spades J looks too modern. Waiting on the player's verdict, then: webp 528×760 into `public/assets/cards/` and draw them in `CardView.ts`. Balance 13.
  - **The player rejected that card set** ("make it a playing card, not a painting"; didn't like the back) and asked to agree on one design first. **New design, 2 images (4 cr):** a graphic printed-deck style (even ink outlines, flat fills, 5-colour palette red/turquoise/gold/ink/cream, cream background). Only the **upper half** is generated (`b22fe568-76bd-45f9-a7d8-34fd6c36eba2`, K♥, 4:3; crop above the ground line at y≈1652) and the code mirrors it 180° into a double-headed court card inside a framed panel, with the rank and suit in two corners. Back `f88ad02c-e1a9-4431-a7e9-8e9302daf52f`: terracotta field, fine cream sadu lattice, small gold sun medallion. Mock-up made with PIL (panel x 96..432 of 528, corner index column 96 px). Waiting on the player's verdict before the other 11 faces. Balance 9.
  - **Also rejected.** The player's pick: the **mythology card set** look (per-suit flat colour field, white diagonal corners for the index, big expressive figure, bold lineart + cel shading) **mirrored** top-bottom. K♥ in that style `67f1fad4-190c-4fdd-8936-1f9803254fff`: the style is right, but it's framed too tight (the head fills the width), so the diagonal mirror cuts through the beard — regenerate with the head about a third of the width, down to the waist. **Back from the player's references** (engraved 19th-century card backs, one ink colour on cream): `33af2a02-a25e-4a3b-8074-632a7b68d31a`, navy #243B5A on cream, central sun + zellige star medallion, two mirrored keys, palms and roses, no text — looks right. Balance 5.
  - **Player's verdict:** back approved; the cel-shaded king "very bad"; **drop the mirror effect** (one upright figure). New style references from the player (vintage European comic / Art Nouveau poster: fine ink + hatching, flat rich colour fields with print grain, teal/navy vs orange/rust/gold, a big dignified figure in an ornamental arch or pattern). Described in words only — the images carry a living artist's signature, so they weren't uploaded. K♥ in that style: `3c5f755b-a35f-45ab-b0fe-50b29f160d86` (teal field, Art Nouveau gold arch, halo disc, red and cream roses; rank/suit on cream plates in two corners by code). Open point: the halo can read as a saint's icon (§5) — swap it for the theme's burnished-gold sun disc. Balance 3.
  - **Player: "much nicer"** — asked for a calmer background and a K that belongs to the design. Final K♥ `f469b4c6-dd56-435c-95ee-a68b33af21f5` (edit of `3c5f…` with the prior image as reference: plain teal, roses only low on the sides and bottom, the disc is now a burnished-gold sun with a thin ring). **Corner index, drawn by code:** the rank in Cinzel Bold (Google Fonts, OFL) gold #E3A33B with a 4 px ink stroke, ~108 px on the 528-wide card, and the suit as a vector shape in the suit colour with a gold outline, no plate, sitting in the teal outside the arch; mirrored in the bottom-right corner. Mock-up script: `index.py` logic (to be ported into `CardView.ts`; Cinzel has no suit glyphs, so draw the suits as paths). Balance 1 — the other 11 faces need a top-up (22 credits).
  - **Corner rework (player):** K was clipped at the top, the heart looked pasted on; make the frame's corner step bigger and put the K and suit inside it on clean teal. Done in code, no credits: paint the corner teal (45,74,77), redraw the frame's gold line (gold 6 px over ink 10 px, matching the art's line at x≈45.5 / y≈23.5 on the 528×760 card) with a bigger stepped notch (NX 134, NY 262, radius 16); K in Cinzel Bold 84 px gold with a 3 px ink stroke; the heart drawn like the frame (ink edge, gold band, ink line, rose-red fill, fine gold inner line); everything supersampled ×4; mirrored in the bottom-right. Known issue: the bottom-right notch line crosses the king's sword hand — for the other 11 faces, pass this composited card as the reference so the art leaves that corner free. **Rejected by the player:** painting the notch over the finished art cut the inner arch, left a broken join where the new line meets the old top line, and the bottom-right line slices the robe and roses. Lesson (now in art-direction §6.4): zoom every seam before showing, and ask the model to draw frame shapes itself — never patch them over art in code. Fix = one edit generation of `f469b4c6…` asking for the bigger stepped notch drawn into the frame with the arch redrawn to fit (2 cr; balance 1, needs a top-up).
- **Assets:** when the player uploads a batch, check it against `docs/art-direction.md` §6.4, then crop/convert to webp at the §7 sizes and load it in the scenes (cards first: faces leave the corners empty; the code draws ranks and suits).
- **Three maps (acts)** — the player wants it, "but not now": after the boss, a harder map; الحوت appears at the start of each map (never mid-map) with stronger blessings.
- Playtest on a phone; tune numbers in `baseOptions` (`src/roguelike/jokers.ts`), `opponents.ts` and `blessings.ts`.
- Build `JokerView.ts` to show the joker art (row above the table, shop cards).
- Before pushing: `npm test` and `npm run build` must pass — CI blocks the deploy otherwise. UI changes get a Playwright check at 359×685 (the player's phone).

---
*Note to AI Agent: Please update this file with your progress, edited files, and the next step before you finish your turn so the other model can seamlessly take over. Save it as UTF-8.*
